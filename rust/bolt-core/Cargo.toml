[package]
name = "bolt-core"
version = "0.1.0"
edition = "2021"
description = "Canonical Bolt Protocol core — crypto primitives and utilities"
license = "MIT"
repository = "https://github.com/the9ines/bolt-core-sdk"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
